{
  "name": "dragon-better-history",
  "version": "2.0.0",
  "description": "Better browser history extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "build:chrome": "cross-env BROWSER=chrome vue-tsc --noEmit && cross-env BROWSER=chrome vite build",
    "build:edge": "cross-env BROWSER=edge vue-tsc --noEmit && cross-env BROWSER=edge vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.6.1",
    "@tailwindcss/vite": "^4.0.0",
    "@types/chrome": "^0.0.287",
    "@vitejs/plugin-vue": "^6.0.7",
    "@vue/test-utils": "^2.4.6",
    "cross-env": "^7.0.3",
    "jsdom": "^25.0.1",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "vite": "^8.0.16",
    "vitest": "^4.1.8",
    "vue-tsc": "^2.1.10"
  }
}
